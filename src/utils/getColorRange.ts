//히트맵 데이터 중 최대감정빈도를 찾기
// const maxValue = Math.max(...Object.values(data).flatMap(month => Object.values(month)));

// export const getColorClass = (count: number) => {
//   if (count === 0) return "bg-gray-100"; // 활동 없음
//   const ratio = count / maxValue; // 0 ~ 1
//   if (ratio <= 0.25) return "bg-purple-200";
//   if (ratio <= 0.5) return "bg-purple-400";
//   if (ratio <= 0.75) return "bg-pruple-600";
//   return "bg-purple-800";
// };
